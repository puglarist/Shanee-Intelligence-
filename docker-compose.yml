version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: stronghold-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-stronghold}
      POSTGRES_USER: ${DB_USER:-stronghold}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-stronghold_dev_password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-stronghold}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: stronghold-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:

networks:
  default:
    name: stronghold-network
